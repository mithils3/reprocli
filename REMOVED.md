# Public symbols removed by the code-reduction pass (branch `reduce/pass-1`)

Every entry carries the grep evidence its worker recorded. "Public" here means a CLI flag,
a `python -m` entry point, an exported function imported elsewhere, or a JSON/Supabase field.
Search scope for every grep: `src tests tools scripts prompts .claude/skills README.md tasks`.

## CLI flags and entry points
- `python -m reprocli_repro.batch_runs` entry point + `resolve_bundles`, `parse_args`, `main` (src/reprocli_repro/batch_runs.py) — repo-wide `grep -rn "batch_runs" --include="*" -l` (minus .git/__pycache__) hits only PLAN.md, BASELINE.md, paper_latex/appendix/h_running.tex (a `% src:` comment citing lines 1-20, the docstring), tools/run_viewer/public/merge.js (prose comment), src/reprocli_claude/__main__.py (imports only Run, fetch_runs, select_runs, bundle_for, newest_bundle), and the two test files. `resolve_bundles` had exactly one non-self caller: the deleted `main`. CLI flags removed with it: `--batch --runs-dir --supabase-url --include-running --skip-audited --limit`.
- `python -m reprocli_openai.recheck` entry point (`main`, `run`, `client`, `status`, `call_with_retry`, `retry_after_seconds`, `request_kwargs`, `paper_texts_from_trace`, `MODEL`, `WORKERS`, `MAX_RETRIES`, `REASONING_EFFORT`, `MAX_OUTPUT_TOKENS`, `REQUEST_TIMEOUT`, `TRACE`, `OUT_DIR`, `PROMPT_FILE`, `PAPER_START`, `PAPER_END`) in `src/reprocli_openai/recheck.py` — the one-shot gpt-5.5 recheck runner. `grep -rno "recheck\.[a-zA-Z_]*" tools scripts tests src .claude/skills` and `grep -rn "reprocli_openai" src tests tools scripts prompts .claude/skills README.md tasks` show the only importers are `tools/verify_app/{publish,report}_openai_recheck.py`, `tools/{merge_repin_into_splits,plot_audit_pool,rebuild_splits_from_app}.py` and `tools/verify_app/build_data.py`, which touch only `iter_jsonl`, `write_jsonl`, `raw_rows`-backed `completed_raw_rows`, `output_text`, `hard_no_code_ids`, `add_recheck_args`, `wait_for_recheck`, `collect_recheck`, `POOL`, `RAW_NAME`. No tests exist for the package. `tasks/REFACTOR_PLAN.md:120` already scopes it for deletion and notes `OUT_DIR` points at a directory that no longer exists.
- `--stream-first-response` CLI flag (src/reprocli_vllm/config/cli_args.py) and the SSE client it drove (`stream_chat_completion`, `post_streaming_chat_completion`, `StreamedResponseBuilder` in src/reprocli_vllm/vllm/client.py, plus the `stream=` keyword of `post_chat_completion_row`). Justification: `grep -rn "stream-first-response\|stream_first_response\|StreamedResponseBuilder\|post_streaming_chat_completion" src tests tools scripts prompts .claude/skills README.md tasks docs` → only the `add_argument` itself, the tool_loop line reading it, the client definitions, and one test. No sbatch script or tool passes it; `reprocli_repro/loop.py:114` calls `post_chat_completion_row` without `stream`.
- `--num-prompts` CLI flag (src/reprocli_vllm/config/cli_args.py) and `run_arxiv_prompt_vllm.select_papers` (with its `import random`). Justification: `grep -rn "num-prompts|num_prompts|select_papers" src tests tools scripts prompts .claude/skills README.md tasks` → the add_argument, its own validator, a "deliberately absent" docstring note in tests/smoke/test_cli_smoke.py:15, a kill-list entry in tasks/REFACTOR_PLAN.md:79, and three tasks/serve-*.md hits that are vLLM's own benchmark_serving.py, not this CLI. Every real invocation under scripts/reproduce/** passes --paper-ids-file instead.
- `--data-parallel-size`, `--data-parallel-size-local`, `--data-parallel-start-rank`, `--data-parallel-address`, `--data-parallel-rpc-port` CLI flags and `_dataparallel_flags()` in `src/reprocli_serve/{args,launch}.py` — the wide-EP rendezvous was never launched. `grep -rn "data-parallel\|data_parallel" scripts tasks README.md .claude/skills tools tests src prompts` hits only the definition/emission lines inside `reprocli_serve` plus one prose mention in `prompts/prompt_reproduce.txt:155`. Still reachable through the verbatim `-- <passthrough>` tail.
- `--tokenizer-mode` and `--structured-outputs-backend` CLI flags in `src/reprocli_serve/{args,launch}.py` — no profile field backs either, so there is no fallback path. `grep` finds `--tokenizer-mode` only in comments saying it is deliberately not passed (`profiles.py:174`, `scripts/reproduce/dsv4_flash/easy_dsv4_flash.sbatch:34`) and `--structured-outputs-backend` only in `tasks/REFACTOR_PLAN.md:86`, which lists the `reprocli_vllm` flag of the same name. Both remain reachable through the `--` passthrough tail.
- `--vllm-bin` CLI flag in `src/reprocli_serve/args.py` — `grep -rn "vllm-bin\|vllm_bin"` across src, tests, tools, scripts, prompts, .claude/skills, README.md and tasks returns only the `add_argument` line and `launch.py:23`'s read. Replaced by the literal `"vllm"`.

## Functions, parameters, fields, and constants
- `reprocli_repro.report` package -> module: `src/reprocli_repro/report/__init__.py` deleted (pure re-export shim). Import path `reprocli_repro.report` unchanged for all four importers (`loop.py:44`, `cli_args.py:28`, `tests/repro/test_report.py:13`, `tests/repro/test_audit_bundle.py:32`); only the shim itself imported `reprocli_repro.report.report`.
- `report.measurement_schema()` (was `src/reprocli_repro/report/report.py:45`): nullary dict-literal helper, inlined at its one call site. grep over src tests tools scripts prompts .claude/skills README.md tasks: only the def, the one call, and its own `__all__` entry.
- `report.__all__` (was `report/report.py:217-228`): `grep -rn "import \*" src tests tools scripts prompts tasks README.md` returns nothing.
- `prompt_render.render_reproduce_prompt(run_paths=)` keyword parameter: unread anywhere in `_replacements`; callers `inputs.py:124` and `tests/repro/test_inputs.py` updated. No sbatch/shell caller: the function is not a CLI surface.
- `env.env_inner` (was `src/reprocli_repro/env.py:30`): inlined into `exec_argv`. grep over src tests tools scripts prompts .claude/skills README.md tasks: only the def and `env.py:60`.
- `reference.load_bundle`, `reference.arxiv_matches` (was `reference.py:25,38`): inlined into `find_bundle_row`. grep over src tests tools scripts prompts .claude/skills README.md tasks: only the defs and `reference.py:161,163`.
- `budget.charge` (was `src/reprocli_repro/budget.py:67`): zero production callers. grep for `\bcharge\b` over src tests tools scripts prompts .claude/skills README.md tasks hits only the def, `tests/repro/test_budget.py:11,61`, and prose in docstrings/shell comments. Live path is `gpu_session.py:99` (`step_cost_hours` + `Budget.consume`) and `tools/run_gpu.py:133` (`affordable`).
- `cluster_names()` (src/reprocli_repro/cluster.py) — repo-wide `grep -rn "cluster_names" --include="*" -l` (minus .git/__pycache__) hits only PLAN.md, its own definition, and tests/repro/test_cluster.py. `resolve_cluster`'s error message joins `_PROFILES` directly and `cluster_defaults()` iterates `_PROFILES.items()`, so nothing in src, sbatch scripts, tools, or .claude/skills calls it.
- `reprocli_repro.tools.fetch` (module) with `fetch_url`, `FETCH_TOOLS`, `FETCH_TOOL_HANDLERS` — src/reprocli_repro/tools/fetch.py — pure adapter: a 3-line wrapper that ignored `ctx`, a one-entry handler dict, and one schema. Only importer was `tools/__init__.py`; schema and registered tool name `fetch_url` preserved verbatim there as `_FETCH_TOOL` + a `(arguments, _ctx)` lambda.
- `reprocli_repro.tools._DEFAULT_GPUS_PER_NODE` and the `build_repro_tools(gpus_per_node=...)` default — src/reprocli_repro/tools/__init__.py — an import-time `resolve_cluster(DEFAULT_CLUSTER)` call no caller relied on; cli_args.py:280 and both tests pass the value explicitly.
- `reprocli_repro.tools.files.locate_by_suffix` — src/reprocli_repro/tools/files.py — one caller (`resolve_by_suffix`); inlined.
- `run_health.finalize_extracted_row`, `verification_status`, `signal_verification_states`, `without_score`, `UNVERIFIED_SIGNAL_STATES`, `INCOMPLETE`, `WEB_VERIFICATION_ALIAS`, `VERIFIED` (src/reprocli_vllm/runtime/run_health.py) and `h100.audit_h100_fields`, `legacy_audit_fields`, `basis_text` (src/reprocli_vllm/audit/h100.py). Justification: `finalize_extracted_row` was reachable only from `io.extracted_response`'s non-audit branch; the auditor CLI declares `choices=("audit",), default="audit"` (config/cli_args.py) and every production caller passes `"audit"` (tool_loop.py, reprocli_claude/__main__.py:174). `grep -rn --include='*.py' --include='*.js' --include='*.sh' "audit_h100_fields|legacy_audit_fields|basis_text|signal_verification_states|without_score|WEB_VERIFICATION_ALIAS" src tests tools scripts prompts .claude/skills README.md tasks` → only their own definitions and the tests deleted with them. `h100.recomputed_hours`, `arithmetic_mismatch`, `h100_band`, `H100_BANDS`, `BAND_MAX_HOURS`, `band_labels`, `band_max_hours`, `band_ladder_text`, `as_number`, `MISMATCH_TOLERANCE` and all of `schema/output.py` are kept.
- JSON fields no longer produced by new runs (they were only ever written by the unreachable classifier finalizer): `h100_estimate_basis`, `h100_needs_human_review`, `h100_recomputed_hours`, `h100_arithmetic_mismatch`, `reported_web_verification`. `tools/verify_app/public/{app,json_view}.js` and `tools/verify_app/build_data.py` still read them defensively from historical rows, which keeps working.
- `config.config.FINAL_NO_TOOLS_MESSAGE` (src/reprocli_vllm/config/config.py). Justification: `grep -rn "FINAL_NO_TOOLS_MESSAGE" src tests tools scripts prompts .claude/skills README.md tasks` shows it only as the default argument of `final_user_message`/`conversation_for_round`/`append_final_message`; all four call sites (tool_loop.py:69,216 and reprocli_repro/loop.py:96,473) pass `final_message=args.final_no_tools_message`, which `resolve_mode_settings` always sets to `AUDIT_FINAL_NO_TOOLS_MESSAGE` (or `REPRO_FINAL_NO_TOOLS_MESSAGE`). Its text is classifier prose that would be wrong if it ever fired.
- `timeout` keyword of `endpoint.fetch_model_cards` / `fetch_served_models` / `fetch_served_context_length` / `resolve_served_model`, and the `cli_value` parameter of `endpoint.resolve_api_key` / `auth_headers` (src/reprocli_vllm/vllm/endpoint.py). Justification: `grep -rn "fetch_model_cards|fetch_served_models|fetch_served_context_length|resolve_served_model|resolve_api_key|auth_headers" src tests tools scripts` — no caller ever supplies `timeout` (run_arxiv_prompt_vllm.py:55, reprocli_repro/__main__.py:86,89, scripts/reproduce/muse_spark/muse_spark_sweep.sh:172 all pass base_url and at most model/cli name), and every `auth_headers()`/`resolve_api_key()` call is bare; there is no --api-key flag. `resolve_served_model`'s `cli_value` is kept (it carries --served-model-name).
- `trace_io.trace_row` (src/reprocli_vllm/runtime/trace_io.py). Justification: `grep -rn "trace_row" src tests tools scripts` → only `append_trace_row`'s own call on the line above it; `tools/verify_app/publish_openai_recheck.py`'s `openai_trace_row` is a different function. Inlined into `append_trace_row`; the emitted JSON row is byte-identical.
- `Profile.extra` field in `src/reprocli_serve/profiles.py` — `grep -rn "\.extra\b\|extra=" src tests tools scripts --include=*.py` (excluding `extra_vllm_args`) returns nothing: no reader, no `Profile(...)` construction sets it. Its removal also frees the `field` import, the only use in the file.
- `AuditResult.paper_id` field in `src/reprocli_claude/agent.py` — written at both construction sites and never read. `grep -rn "paper_id" src tests tools scripts prompts .claude/skills README.md tasks` shows no `result.paper_id`/`.paper_id` read on an `AuditResult`; `__main__.audit_run` takes the id from `run.arxiv_id`. The `run_audit(paper_id=...)` keyword stays, so no caller changes.
- `PRICES["claude-opus-4-7"]` and `PRICES["claude-opus-4-6"]` in `src/reprocli_claude/agent.py` — both are `_tier(5e-6, 25e-6)`, identical to the `PRICES["claude-opus-4-8"]` fallback `prices_for` already returns for an unknown name, so deleting them changes no output. `grep -rn "claude-opus-4-7\|claude-opus-4-6"` finds no other hit in the tree.

## Cross-partition step (WP-S)
- `reprocli_repro.context.ExecutionContext.allocation` (write-only dataclass field). Evidence: `grep -rn "allocation" src tools scripts .claude/skills --include=*.py --include=*.sh --include=*.sbatch --include=*.js --include=*.md` -> only the three writers in `gpu_session.py` (78, 112, 133) plus prose; zero reads. Live value is `ctx.session.jobid`.
- `cluster` parameter of `reprocli_repro.slurm.build_srun` and `slurm.run_in_session` (signature change, functions kept). Evidence: `ruff --select ARG` flagged `ARG001 Unused function argument: cluster` at `slurm.py:161`; `grep -rn "build_srun\|run_in_session" src tests tools scripts .claude/skills` -> one production call (`tools/run_gpu.py:152`) plus test call sites only.
- `reprocli_repro.slurm.SessionHandle.command` (write-only dataclass field). Evidence: `grep -rn "SessionHandle" src tests tools scripts --include=*.py` -> written only at `slurm.py:230,238`, never read; the one `.command` read in the package is `step.command` on `StepResult` (`tools/run_gpu.py:279`), which is kept.
- `reprocli_repro.cluster.from_args` (two-getattr passthrough to `resolve_cluster`). Evidence: `grep -rn "from_args" src tests tools scripts .claude/skills README.md tasks` -> the definition, one production import that renamed it to the callee (`cli_args.py:25 from ... import from_args as resolve_cluster`), and `tests/repro/test_cluster.py`; the `from_args` hits in `reprocli_serve/launch.py` are an unrelated local variable.
- `mode` parameter of `reprocli_vllm.vllm.io.extracted_response` (function kept). Evidence: WP-D deleted the non-audit branch, so the body no longer reads `mode`; `grep -rn "extracted_response" src tests tools scripts --include=*.py` -> three `tool_loop.py` calls passing `args.mode`, one `reprocli_claude/__main__.py` call passing the literal `"audit"`, one test. The `--mode` CLI flag is kept (sbatch scripts pass `--mode audit`).
- `paper_id` keyword parameter of `reprocli_claude.agent.run_audit` (function kept). Evidence: WP-E removed `AuditResult.paper_id`, and `grep -rn "paper_id" src/reprocli_claude tests/claude` shows the name is never read inside `run_audit`'s body; the one production call (`reprocli_claude/__main__.py:160`) passed `run.arxiv_id`, which is already carried by the sink bridge and `response_row`.
- `tools/anon_viewer/export.py::NARRATIVE` (module-level constant). Evidence: `grep -rn "NARRATIVE" src tests tools scripts prompts .claude/skills README.md tasks notes --include=*.py --include=*.sh --include=*.sbatch --include=*.js --include=*.md --include=*.txt` -> only its own definition line; `map_narrative()` hard-codes the same six field paths.
- `out_dir` parameter of `tools/merge_repin_into_splits.py::_download_base` (private helper kept). Evidence: `ruff --select ARG` ARG001; `grep -rn "_download_base" src tests tools scripts .claude/skills README.md tasks` -> definition plus one call site.
