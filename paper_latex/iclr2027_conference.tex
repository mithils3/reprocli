\RequirePackage{fix-cm}  % CM math fonts at the 6.5pt listing size
\documentclass{article} % For LaTeX2e
\usepackage{iclr2027_conference,times}

% Common packages. These do not change the ICLR style parameters.
\usepackage[hidelinks]{hyperref}
\usepackage{url}
% URLs break only after / ? = & _, never inside a word, at a hyphen or at a dot. The colon keeps its
% relation class (so // stays together) but its break penalty is raised to infinity.
\def\UrlBreaks{\do\/\do\?\do\=\do\&\do\_}
\def\UrlBigBreaks{}
% Punctuation left out of every url list falls back to the math fonts, so list it as ordinary.
\def\UrlOrds{\do\*\do\-\do\~\do\'\do\"\do\.\do\@\do\\\do\!\do\|\do\;\do\>\do\]\do\)\do\,\do\+\do\#\do\<\do\[\do\(}
\mathchardef\UrlBigBreakPenalty=10000
\renewcommand{\UrlFont}{\small\ttfamily}  % 9pt Courier sits level with 10pt Times and lets long URLs fit a line
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{listings}
\input{appendix/style}  % code, prompt, and schema boxes for the appendices
\usepackage{microtype}
\usepackage{etoolbox}
\usepackage{needspace}
\usepackage{placeins}  % \FloatBarrier keeps each run card inside its vignette
% Table captions sit below the tabular: give them clearance from the bottom rule.
\AtBeginEnvironment{table}{\setlength{\abovecaptionskip}{6pt}\setlength{\belowcaptionskip}{0pt}}
\setlength{\LTcapwidth}{\dimexpr\textwidth-2.3pt\relax}  % longtable captions span the text block (longtable centres them 1.1pt left)
\makeatletter
\patchcmd{\LT@makecaption}{\reset@font}{\reset@font\normalsize}{}{}  % caption stays 10pt inside \small tables
\makeatother

\title{RECLAIM: Can Agents Reproduce the Claims of Machine Learning Papers?}

\author{Anonymous Authors}

% Uncomment only for the camera-ready version after acceptance.
% \iclrfinalcopy

\begin{document}

\maketitle

\begin{abstract}
AI agents can now take released code, set up its environment, run the
experiments, and write up the results. Existing benchmarks prepare the agent's
environment and saturate quickly, whereas reproducing a published paper
involves no such preparation. We introduce RECLAIM, 100 NeurIPS 2025 papers
with a metered GPU-hour budget, a frozen target claim, a numeric tolerance,
and a pinned auditor of execution evidence. We give the agent the paper and
whatever the authors released, and it supplies the rest. For the strongest of
four agents, reproduction rates fall from 48\% at Run tier, where code, data, and
weights are released, to 32\% at Retrain and 13\% at Reimplement, where the
method is built from the paper alone. Compute does not explain the decline,
since non-reproduced runs use only 30\% of their allocation. Labeling every
graded run with one of nine modes shows that for the strongest agent at
Retrain tier most non-reproduced runs ran the paper's experiment but failed
to produce a number inside tolerance. We read this as evidence that current agents are
competent executors but weak research scientists, separated from agents that
automate research by the judgment that turns an executed experiment into a
confirmed number.
% numbers of record, filled 2026-08-24. All pinned claude-sonnet-5, post-freeze:
%   tier fall 48/32/13 -- DSv4-Flash easy 14/29 (2883229+2889575), medium 9/28
%     (2896059), hard 4/30 (2918306, pinned re-audit + dissection 2026-08-12,
%     which cleared the old "Reimplement has no pinned number" blocker). Scoped
%     in-sentence to the strongest agent: the fall is DSv4's, never pooled.
%   "rarely runs as shipped" -- CUT 2026-09-06: unsourced frequency claim, and
%     the body attributes Run-tier misses to the agent. Run tier is now defined
%     inside the tier sentence instead.
%   Run-tier environment-repair clause -- CUT, per the prior TODO: the transcript
%     pass over the 14 reproduced easy runs was never run, and the instruction
%     was to cut rather than soften. Repair evidence stays in the body (five
%     shipped-code defects, easy dissection section 2).
%   failed-run spend 30% -- mean spent_h100/budget over the 299 non-reproduced
%     runs pooled across the 12 sweeps of record, all four agents (98 run + 89
%     retrain + 112 reimplement), computed 2026-09-03 from
%     tools/anon_viewer/public/data/index.json; median 15.7%, ratio of sums
%     18.6%. DSv4 alone is 45.2% (n=60), the figure this line carried until
%     2026-09-03. Absolute claim only: reproduced runs pool to 27.1% (n=73),
%     so never imply failures are cheaper.
%   Retrain misses 15 of 19 -- near-miss-partial 15 of 28 on 2896059 against 19
%     misses (28 minus 9 reproduced). DISSECTION-derived, not auditor-derived,
%     and the body must say so where it appears. 2026-09-04: the abstract now
%     says "most misses" for the strongest agent and drops the two counts and
%     the 16-32% range (user: unclear, does not belong in the abstract); the
%     numbers stay in the body. If the body reuses the older
%     right-experiment rate (24/28 dissection vs 22/28 audit_score>=6), pick one
%     there and state which.
%   matched-number range 16-32% -- Retrain tier, pinned Sonnet-5: DSv4 9/28=32%,
%     Qwen3.6 6/26=23% (2698678+2759663), MiniMax 5/32=16% (2690187), Muse Spark
%     9/32=28% (muse_spark 20260816T133804Z, added 2026-09-03). The four
%     do not agree, so the sentence carried the range per the prior TODO, and
%     the flat "stays at" claim is retired. 2026-09-04: the range left the
%     abstract (user: unclear); it now appears in the body only.
%   audit-score range (intro) -- 3.41 (MiniMax) to 6.43 (DSv4) is 1.9x; Muse Spark
%     6.03 sits inside the range (2026-09-03), so the 1.9x stands. Filled
%     2026-08-27 together with the matched-number range 16-32%, which replaces
%     the retired flat "holding at" claim in the same sentence.
%   Retrain verified-partial 79% (intro) -- 22/28 with audit_score>=6 on
%     2896059, auditor-derived under the frozen rubric. The 24/28 dissection
%     right-experiment rate is deliberately not used there; anywhere the body
%     uses 24/28 it must state the dissection provenance.
%   96 GPU-hour band spend -- 6.5% (sum spent / sum budget; the mean of per-run
%     fractions is also 6.5%), pooled over the 12 sweeps of record incl. Muse
%     Spark, 1 of 42 runs reproduced (Muse Spark, Retrain, 2508.21046, score 9,
%     1.9 of 96 H100-h). Refilled 2026-09-03; matches tab:band-compute.
\end{abstract}

\section{Introduction}
\label{sec:introduction}

AI agents do a growing share of the engineering behind machine learning
research. They implement, execute, and debug large-scale experiments and
software projects~\citep{jimenez2024swebench,chan2024mlebench,
merrill2026terminalbench}, and they are already deployed on this work
in the real world~\citep{handa2025economic,huang2025aiwork}. As their capabilities rise,
static benchmarks tend to saturate soon after they are
released~\citep{kiela2021dynabench,phan2025hle}, so evaluation has to move
closer to the workflows agents are deployed into. Reproducing a published ML paper by
obtaining results consistent with those its authors report is an ideal
ground for that evaluation, both for its direct value to the field and for
its difficulty~\citep{siegel2024corebench,starace2025paperbench}. In ML
research, published results are often not easy to reproduce. One sustained
replication effort reproduced only 162 of 255 attempted papers without
using the authors' code~\citep{raff2019step}, and independent studies
attribute the shortfall to missing code, missing data, and underspecified
experimental detail~\citep{gundersen2018state,pineau2021improving,
gundersen2022sources}. Reproduction is also a prerequisite for recursively
self-improving systems, since a model that improves itself must first
re-implement and verify the research it builds
on~\citep{favaro2026recursive}. Both Anthropic and OpenAI report that their
researchers delegate the running of experiments to agents and retain the
judgment of which results to
trust~\citep{favaro2026recursive,openai2026acceleration}.

To support this evaluation, we introduce RECLAIM,\footnote{\renewcommand{\UrlFont}{\fontsize{8}{9}\selectfont\ttfamily}Code:
\textcolor{blue}{\href{https://anonymous.4open.science/r/reclaim-5733}{\nolinkurl{anonymous.4open.science/r/reclaim-5733}}}.
Run logs: \textcolor{blue}{\href{https://reclaim-traces.vercel.app}{\nolinkurl{reclaim-traces.vercel.app}}}.\label{fn:artifacts}}
a benchmark that measures
whether an agent can reproduce a published result end to end, starting from the
artifacts the paper's authors actually released and graded against the
execution evidence the run produces. Our results show that reproduction rate
follows how much the authors release. The strongest agent we evaluate, DeepSeek-V4, recovers
the target result on 48\% of Run-tier papers (code, data, and weights all
released), 32\% of Retrain-tier papers (no released weights, so the model must
be trained first), and 13\% of Reimplement-tier papers (no released code
either). We call
this gradient the availability cliff, and it is the axis the benchmark is
constructed to vary.

The cliff sets a ceiling and leaves the shortfall beneath it unexplained. At the
Run tier the authors released every artifact the claim requires and we verified
each one retrievable by direct host probe, and many of those claims still do not
come back. Three properties of that residual organize the rest of this paper.
Stronger agents achieve partial reproduction more often. On Retrain-tier
papers the strongest agent reaches a verified partial result or better on 79\% of runs,
which provides evidence of an emerging ability to close the loop of
reproduction. Full recovery of
the reported number is where progress stalls, holding between 16\% and 32\%
across four agents spanning a 1.9-fold range in mean audit score, so the gain
between agent generations shows up almost entirely in how far into the
reproduction an agent gets. Compute does not bind either, since the 42 runs in the 96 GPU-hour band spend 6.5\%
of the allocation they were given and 1 of the 42 reproduces its claim.

A small set of recurring failure modes accounts for the residual, and the modes
divide by whether they follow agent capability. Runs that reimplemented the
method but did not check the result against anything the authors released
account for 43 of the 188 runs of the two weakest agents and 20 of the 184 runs
of the two strongest, and build and dependency failures fall from 19 of those
runs to 5.
Measuring the wrong artifact does not follow capability. The two strongest
agents account for 21 of its 38 cases, and on 7 papers the released model card
or configuration file states a setting that differs from the one the paper
reports. We define the taxonomy, report the per-mode
counts, and give the transcript evidence in Section~\ref{subsec:mechanisms}. Agent
self-reports track none of this reliably, so we carry claimed and audited counts
as background data rather than as a result.
% numbers of record (2026-09-05): resolved per-run primary modes in
% ~/sweeps/paper-table-2026-09-05/resolved_modes.json (index.json of 2026-09-03
% plus the 29-run relabel pass in result.json). Agents ordered by mean audit
% score: DSv4 5.90, Muse 5.15, Qwen3 3.88, MiniMax 3.09. reimplement-without-
% validating: Qwen3 16/90 + MiniMax 27/98 = 43/188; DSv4 13/87 + Muse 7/97 =
% 20/184. environment-fights: 8+11 = 19 vs 1+4 = 5. artifact-provenance-
% mismatch per agent 6/87, 3/90, 13/98, 15/97 = 37; release-disagrees-with-paper
% signature in 10 runs on 7 papers (dsv4-retrain-2506.19839, dsv4-run-2505.23305,
% dsv4-run-2510.21311, dsv4-run-2505.24873, muse-run-2505.23305,
% muse-run-2511.16666, muse-run-2505.18456, minimax-run-2511.16666,
% minimax-run-2505.18456, minimax-run-2505.10475), read from failure_mode_detail.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/reclaim_overview}
\caption{Overview of RECLAIM and its evaluation pipeline. The figure shows an
example run with DeepSeek-V4 on the CQ-DINO paper.}
\label{fig:overview}
\end{figure}


\section{Related Work}
\label{sec:related-work}

\paragraph{Repository and terminal agents.}
Agent evaluations have moved from isolated code generation to long-horizon
work inside real repositories and terminals~\citep{jimenez2024swebench,
merrill2026terminalbench,miserendino2025swelancer,deng2025swebenchpro,
lu2026frontiercode}. SWE-bench grades patches for real GitHub issues using
the tests the repository already ships~\citep{jimenez2024swebench}, and
Terminal-Bench 2.0 poses command-line tasks inside prepared containers with
executable checks written in advance~\citep{merrill2026terminalbench}. Both
designs scale precisely because the objective, the environment, and the
verifier are all fixed before the agent starts, but that same property
removes the first half of a reproduction, where an agent has to decide
which experiment establishes the claim and build an environment that can
run it before any number exists to check. MirrorCode pushes the setting
furthest and asks agents to rebuild entire programs from behavior alone,
with a black-box reference implementation available to query
throughout~\citep{adamczewski2026mirrorcode}. That reference implementation
is the operative difference between its setting and ours. An agent can query
it for the correct output on any input it likes, so its target is checkable
at every step. An agent reproducing a paper has nothing to check itself
against until the experiment already runs. Agents stop with budget to spare
in both settings. \citet{adamczewski2026mirrorcode} report that 97.4\% of
their runs submitted before using half the token budget, and describe agents
that submit once the visible tests pass. We report the same stopping
behavior in Section~\ref{subsec:results}, where no such test exists to pass.

\paragraph{ML engineering and experimentation.}
A second family targets the machine-learning workflow itself, from model
development through research engineering~\citep{huang2024mlagentbench,
nathani2025mlgym,tang2024mlbench,jing2025dsbench,zhang2025datascibench,
wijk2024rebench,zhang2025mlrcbench,lupidi2026airsbench}. MLE-bench is
representative, with agents building and refining pipelines for Kaggle
competitions under a fixed compute budget and each submission scored
against the historical leaderboard for that
competition~\citep{chan2024mlebench}. Because the method is the agent's to
choose and success is a leaderboard position rather than a specific
published quantity, these benchmarks reward any solution that scores well
and leave open whether an agent can carry a prescribed method and
configuration into an environment its authors never used and land on the
number they reported.

\paragraph{Scientific reproduction.}
A growing line of benchmarks measures how well agents reproduce published
research~\citep{siegel2024corebench,starace2025paperbench,bogin2024super,
xiang2025scireplicate,kon2025expbench,hua2025researchcodebench,
edwards2025rexbench,zhao2025speedrunning,nguyen2026replicatorbench,
wang2026researchenvbench,seo2025paper2code,zhao2025autoreproduce}, and
three of them bracket the design space. CORE-Bench asks agents to rerun the
released code of computationally reproducible papers and recover known
artifacts~\citep{siegel2024corebench}, a setting frontier models have since
effectively saturated~\citep{favaro2026recursive}. PaperBench lies at the
opposite extreme and has agents replicate 20 ICML 2024 papers from scratch
with the authors' code withheld, grading the attempt against hierarchical
rubrics the original authors wrote~\citep{starace2025paperbench}.
EXP-Bench withholds the paper itself and hands the agent a research
question and starter code to design and run the experiments
from~\citep{kon2025expbench}. In all three the release state is settled
before the run begins, usually by selection, since most keep only papers
whose code is already public and already runs, and the stricter protocols
exclude the rest on the stated reasoning that an irreproducible paper is
not worth including. That builds a clean coding task, but it fixes the
variable RECLAIM exists to measure, and a benchmark assembled only from
papers that shipped working code cannot say what happens on the papers that
did not.

\section{RECLAIM}
\label{sec:reclaim}

RECLAIM (Reproducing Claims in Machine Learning) evaluates whether an
autonomous agent can reproduce the central
empirical result of a machine learning paper from whatever artifacts its
authors actually released. Each of the 100 papers in the benchmark poses one
task, and the agent must recover a single pinned result within a stated
tolerance.
\subsection{Features of RECLAIM}
\label{subsec:features}

\paragraph{End-to-end reproduction.} Each task hands the agent a recent
paper and a target. No scripted pipeline or prepared environment is
provided. The agent plans the reproduction, installs the paper's stack and
reconciles its dependency errors, debugs failures, and manages its own GPU
allocations, the same work a researcher faces when reproducing a paper. One
open-ended task therefore exercises planning, coding, and resource
management together. RECLAIM complements prior computational-reproducibility
benchmarks~\citep{siegel2024corebench} by evaluating the less structured case
in which an agent must recover and execute an experiment from the paper and its
naturally released artifacts.

\paragraph{Natural artifact availability.} We stratify papers by the artifacts
their authors released, so each tier reflects a reproduction setting that
actually occurs in the field, and no difficulty is manufactured by
withholding resources. This choice also limits contamination.
Reimplement-tier papers never released code, so no reference implementation
exists for a model to absorb during training, and across all tiers our
cluster seldom matches the authors' hardware and software stack exactly, so
replaying memorized artifacts is rarely enough.

\paragraph{Sufficient compute.} Each paper carries a GPU grant estimated from
its minimal reproducible experiment, with additional capacity for iteration. As
reported in Section~\ref{subsec:results}, most agent runs do not exhaust their
assigned compute. Although this does not establish sufficiency for every task,
it indicates that the compute cap is not the binding constraint for most runs.

\paragraph{Provenance-audited grading.} The benchmark records what the
agent claims and grades what the evidence shows. Every reported number is
traced to execution evidence, and deterministic checks zero out any number
that was echoed from the paper and never measured. Because the audit checks how
the number was produced, the
agent is free to reach the result by any faithful route, and a novel
reproduction strategy scores as well as the authors' own. Recording the
claim and the audited outcome side by side is what lets us measure how far
agent self-reports diverge from verified reproductions.

\paragraph{Refresh at low human cost.} An agent classifier reads and
verifies the candidate papers, and humans audit only the selected pool
(Section~\ref{subsec:construction}). Each conference cycle supplies fresh
peer-reviewed targets, so the benchmark can be rebuilt yearly from new
NeurIPS, ICML, and ICLR submissions. The pool tracks the research frontier
and resists saturation as agents improve.

\subsection{Task definition}
\label{subsec:task}

Each paper carries one reproduction target, which we call the \emph{match
target} and fix before any agent runs. The match target is the cheapest
configuration the authors report that still establishes the paper's central
empirical claim, together with the value that configuration produces. Papers
usually headline their largest model or their full benchmark suite, but the
same claim typically rests on a smaller supporting experiment, and we pin that
one. Pinning the smallest supporting configuration bounds the compute a task
can demand while still exercising the paper's method end to end.

A match target records the configuration that produces the value, the metric,
the reported value, the scope the value is measured over, and the bar a run
must clear, most often a point estimate within a stated or default tolerance.
Appendix~\ref{app:dataset-schema} details the bar kinds and the tolerance
rule.

As a running example, SharpZO (arXiv 2506.20990) claims that a two-stage
zeroth-order method improves prompt tuning for vision-language models using
forward passes alone. Its match target pins a test accuracy of 79.42\% on the
EuroSAT test set under a 16-shot CLIP ViT-B/16 configuration the paper
reports. A run matches when it produces that accuracy, within tolerance, from
an experiment it actually executed.

The agent receives the paper, the metric, the target value, the scope, and
the bar shape. It does not receive the configuration. The agent must
therefore design the experiment itself, deciding from the paper and the
released artifacts what to run, retrain, or reimplement. Because the agent
knows the number it must hit, a bare value match proves nothing. An LLM
auditor therefore traces every claimed result to execution
evidence in the run directory, and deterministic checks force the score to
zero when a number was echoed from the paper and never measured.

\subsection{Benchmark construction}
\label{subsec:construction}

We construct RECLAIM from the 3,414 NeurIPS 2025 submissions that carry an
arXiv ID, collected in the \emph{ai-conferences}
corpus~\citep{neurips2025corpus}. Construction proceeded in three stages
(Figure~\ref{fig:funnel}).

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/construction_funnel}
\caption{The construction funnel. Three stages narrow 3,414 NeurIPS 2025
submissions to the released 100-paper benchmark, with every availability call
and compute estimate audited by hand.}
\label{fig:funnel}
\end{figure}

\textbf{Stage I: Artifact-verified classification.} For each submission we
retrieved the raw LaTeX source together with any OpenReview supplementary
material, since some authors ship code only in the supplement
(Appendix~\ref{app:corpus}).
A classifier agent built on MiniMax-M2.7~\citep{minimax2026m2} read each paper,
extracted its central empirical claim, pinned its match target, and audited four
artifact signals: released code, released datasets, released model weights, and
whether the required datasets are standard public benchmarks
(Appendix~\ref{app:classifier}). An artifact counts
as available only when a tool call is able to access it. An artifact the paper
promises but never publishes counts as missing, as does one behind a paywall, a
sign-up form, or a permission request. We ran the classifier on a 1,000-paper
sample, and it fully verified 687 papers. Removing non-empirical papers and
papers with blocked artifacts leaves 620 eligible for evaluation.

Artifact availability assigns each paper to a tier that records how much of
the result the agent must rebuild. Run papers
release code, data, and weights, and the agent need only execute or evaluate them.
Retrain papers withhold the weights, and the agent must train the model before
evaluating. Reimplement papers withhold the code as well, and the agent must
rebuild the method from the paper alone. Compute cost is
derived from the paper's text if mentioned, or estimated from the MRE config.
The tiers also describe the venue as a whole. Among
the 687 verified papers, 33\% fall in Run, 44\% in Retrain, and 13\% in
Reimplement, while 5\% are blocked outright and 5\% are non-empirical.

\textbf{Stage II: Compute capping and stratified selection.} We estimated the
compute each match target requires in H100-hours, taking the paper's reported
hardware and wall-clock time where stated and deriving the estimate from the
pinned configuration otherwise (Appendix~\ref{app:selection}). We cap the
benchmark at 96 H100-hours and drop every paper estimated above it. We sampled a 200-paper audit pool
stratified by tier and compute band. We allocate the pool equally across
tiers because tier is the variable under test, and equal cells measure each
tier's reproduction rate with equal precision. Proportional sampling would leave the Reimplement tier with roughly a
dozen papers, and the venue proportions reported above let readers reweight
any rate back to the venue level.

\textbf{Stage III: Human audit.} We audited every paper in the pool by hand,
opening the evidence behind each availability call and re-deriving each compute
estimate (Appendices~\ref{app:human-audit} and~\ref{app:recompute}). Papers whose artifacts proved
inaccessible or that depended on paid third-party APIs failed the audit and
were replaced from the audited pool. We
exclude paid APIs so that reproduction cost stays bounded and
GPU-denominated. Only 20 of
the 100 shipped compute estimates could be taken directly from the paper. We
derived the rest from the pinned configuration. The need to reconstruct these
estimates is one instance of the broader documentation gaps that impede
reproduction in AI research~\citep{gundersen2018state}.
\section{Auditing}
\label{sec:auditing}

Every finished run is graded by an LLM auditor that reads the
run's evidence directory and transcript and assigns the 0--10 score of
Appendix~\ref{app:rubric}, from which the categorical verdict is derived
deterministically. We fix Claude Sonnet 5 as the auditor for every run of
every agent, and we froze the rubric before grading began, so scores are
comparable across models and no agent grades its own family. The auditor is
adversarial by instruction. It treats the agent's final report as a set of
unverified claims and grades what the evidence shows. It traces every
reported number to the execution that produced it, checks the executed
protocol, split, and scale against the pinned configuration, and
cross-checks the agent's measurements against the paper's reported values.
When the recorded evidence does not settle a claim, it writes and executes
its own scoring scripts over the run's saved outputs and recomputes
metrics directly from the raw artifacts. It inspects
and recomputes but never re-runs the reproduction, so a grade reflects the
evidence the run actually produced.

The auditor's discretion is bounded in code. Every violation it finds is
recorded as a typed anti-cheat flag with a severity, and the scoring code
applies the integrity adjustment of Appendix~\ref{app:rubric}. A
high-severity flag, such as a number echoed
from the paper, a hardcoded constant, or a proxy value in the target slot,
forces the score to zero, and a claimed artifact wall earns no credit band.
Grading by evidence also preserves the agent's creativity, since any
faithful route to the pinned number earns full credit, including
strategies the authors never used. An
LLM auditor reads more material per run than a human grader can,
working through hundreds of tool rounds, logs, and saved outputs in
minutes, which is what makes auditing every run of every sweep feasible.
We have not validated the auditor against human graders.
Appendix~\ref{app:auditor-validation} instead measures agreement between
graders by re-grading two 32-run sweeps with the pinned auditor after the
agent model had graded its own runs. The reproduced set survives the grader
swap on both sweeps, with one run exchanged on the first and one removed on
the second, and the continuous score correlates at $r = 0.86$ away from the
score-0 cap. Only 4 of the 10 disqualifications on the second sweep are
shared by both graders, so the cap is where the graders disagree, and we
report disqualification counts as a rubric behavior.
% numbers of record: appendix/d_auditor_validation.tex, tab:auditor-validation.
% Easy sweep 2687371 (Qwen3.6 self-graded vs pinned Sonnet-5, n=32): reproduced
% 5 vs 5 with one run lost and one gained. Medium sweep 2690187 (MiniMax
% self-graded vs pinned, n=32): reproduced 6 vs 5, pinned set a strict subset;
% DQ 10 vs 10 sharing 4; r=0.657 over all 32, r=0.863 over the 16 runs where
% neither grader applied the score-0 cap. Human-grader study: none on record
% (2026-09-06, the labeled repo was deleted), so the sentence says so.

\section{Experiments}
\label{sec:experiments}

\subsection{Experimental Setup}
\label{subsec:experimental-setup}

\paragraph{Agent harness.} We run every agent in a thin ReAct-style agent
loop~\citep{yao2023react}. The tools we provide leave the agent free to
design its own experiment. A workspace shell and a metered GPU session express the
same workflows a human reproducer runs on a cluster, and the harness adds no
guidance beyond them, so outcomes measure the model. The agent receives the paper's LaTeX source, its OpenReview
supplement, the verified links to the released artifacts, and the match
target of Section~\ref{subsec:task}. \texttt{run\_gpu} is the only path to a
GPU, holding a Slurm allocation whose wall clock is metered against the
run's fixed H100-hour grant. The agent chooses its own partitions,
allocation sizes, and session lengths, so every run also exercises the
budgeting and scheduling decisions that real reproduction work demands.
Each run ends with a structured report in which the agent states what it
ran, what it measured, and whether it believes the result reproduced. This
self-assessment is a claim for the auditor to grade. Appendix~\ref{app:harness} gives the
prompts, the tools, and session mechanics.

\paragraph{Models.} We evaluate four models:
DeepSeek-V4~\citep{deepseekai2026deepseekv4highlyefficientmilliontoken},
Qwen3.6-27B~\citep{qwen3.6-27b}, MiniMax-M2.7~\citep{minimax2026m2}, and
Muse Spark 1.2~\citep{meta2026musespark}. The first three are open-weight and
served on our cluster with vLLM, and we reach Muse Spark 1.2 through the Meta
Model API. Every model runs the same harness, prompts, and per-paper budgets.
Serving differs by model, and Appendix~\ref{app:models} records pinned
versions, serving configuration, and decoding settings where we control them,
together with what the hosted model leaves unrecorded.

\paragraph{Metrics.} Our primary metric is the mean reproduction score, an
integer grade from 0 to 10 that the auditor assigns each run
under the rubric of Appendix~\ref{app:rubric}. We also report the
reproduction rate, the fraction of papers whose runs score 8 or higher and
are therefore graded reproduced. Alongside both we report each model's token
usage and total benchmark cost, the sum of its API pricing and its GPU time
at \$2 per H100-hour.

\subsection{Results}
\label{subsec:results}

Table~\ref{tab:main-results} reports every graded run. Each agent has between 87
and 98 graded runs over the 100 papers; the remaining cells have no graded run,
and every rate below uses the graded runs as its denominator. DeepSeek-V4 is
missing 13 of its 100 cells and MiniMax-M2.7 two; counting every missing
DeepSeek-V4 cell as a non-reproduction gives 41\%, 27\%, and 12\%, so the
gradient survives the worst case. The auditor
confirms reproduction on 73 of the 372 runs. DeepSeek-V4 is the strongest of
the four agents. It recovers the pinned number on 14 of 29 Run-tier papers, 9
of 28 at Retrain, and 4 of 30 at Reimplement, and the four agents span a factor
of 1.9 in mean audit score, from 5.90 for DeepSeek-V4 to 3.09 for MiniMax-M2.7.
% TODO(source): state why 28 cells have no graded run (2 dropped for lacking a
% pinned grade per tools/anon_viewer/export_report.md; the rest never produced a
% run row). Missing per agent x tier vs 34/33/33 papers: DSv4 5/5/3 (2510.20725
% has no run for any agent), Qwen3 0/7/3, MiniMax 1/1/0, Muse 1/1/1.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}lccc@{\hspace{1.2em}}ccc@{\hspace{1.2em}}c@{}}
\toprule
& \multicolumn{3}{c@{\hspace{1.2em}}}{Reproduced} & \multicolumn{3}{c@{\hspace{1.2em}}}{Mean score} & Grant \\
\cmidrule(lr{1.2em}){2-4} \cmidrule(lr{1.2em}){5-7}
Agent & Run & Retrain & Reimplement & Run & Retrain & Reimplement & spent (\%) \\
\midrule
DeepSeek-V4 & 14/29 (48\%) & 9/28 (32\%) & 4/30 (13\%) & 6.21 & 6.43 & 5.10 & 32.0 \\
Qwen3.6-27B & 5/34 (15\%) & 6/26 (23\%) & 2/30 (7\%) & 3.91 & 4.54 & 3.27 & 15.5 \\
MiniMax-M2.7 & 3/33 (9\%) & 5/32 (16\%) & 2/33 (6\%) & 3.18 & 3.41 & 2.70 & 10.3 \\
Muse Spark 1.2 & 9/33 (27\%) & 9/32 (28\%) & 5/32 (16\%) & 5.64 & 6.03 & 3.78 & 21.7 \\
\midrule
All agents & 31/129 (24\%) & 29/118 (25\%) & 13/125 (10\%) & 4.68 & 5.08 & 3.69 & 19.4 \\
\bottomrule
\end{tabular}
\caption{Reproduced runs, mean audit score, and share of the granted H100-hours
spent, by agent and tier.}
\label{tab:main-results}
\end{table}
% numbers of record (2026-09-05): tools/anon_viewer/public/data/index.json
% sweeps[] (n, n_reproduced, mean_score); reproduced = audit score >= 8 =
% verdict reproduced on all 372 runs. Grant spent = 100 * sum(spent_h100) /
% sum(budget_h100) over the agent's runs: 636.7/1992.0, 324.6/2088.0,
% 259.6/2512.0, 549.4/2528.0, all 1770.2/9120.0. All-agents means are over all
% runs in the tier (4.68/5.08/3.69), not means of the four rows.

The agents differ more in how far they get than in how often they finish. We
call an audit score of 6 or more a partial result or better. At Retrain,
DeepSeek-V4 reaches one on 22 of 28 runs and Muse Spark 1.2 on 23 of 32, while
MiniMax-M2.7 reaches one on 9 of 32, and confirmed reproduction at the same
tier runs from 16\% to 32\%. Only DeepSeek-V4's reproduction rate falls at
every tier. For the other three agents the Run and Retrain rates sit within
nine points of each other, and mean score rises from Run to Retrain for all
four agents before falling at Reimplement. Table~\ref{tab:failure-modes}
shows where the tiers differ. Retrain has more runs that execute a faithful pipeline
and land outside tolerance, 37 of 118 against 24 of 129 at Run, and Run has
more measurements on the wrong released artifact, 18 of 129 against 9 of 118.
Every agent's lowest reproduction rate and lowest mean score are at
Reimplement, where no code is released, and 13 of 125 runs reproduce there.
% partial-or-better = audit score >= 6 (index.json): DSv4 retrain 22/28, Muse
% retrain 23/32, MiniMax retrain 9/32.

Spending stays far below the grant in every row of the table. The mean
non-reproduced run uses 30\% of its allocation, and pooled over all 372 runs
the agents spend 1{,}770 of the 9{,}120 H100-hours we granted, from 32.0\% of
the grant for DeepSeek-V4 to 10.3\% for MiniMax-M2.7. We carry the agents' own verdicts as background data. Of the 359
runs that return a parsable self-assessment, 128 claim reproduction, against 73
audited reproductions over all 372 runs, and the disagreement runs in both
directions. MiniMax-M2.7 claims 43 reproductions and the auditor confirms 10
of its runs, and DeepSeek-V4 claims 19 and the auditor confirms 27.
% self_report field of index.json: reproduced 128, not_reproduced 208, partial
% 23, none 13 (12 of them Qwen3.6-27B). The abstract's 30% is the mean per-run
% spent/budget over the 299 non-reproduced runs; the pooled figure here is
% hours over hours (19.4% all runs, 18.6% non-reproduced).

\subsection{Failure mechanisms}
\label{subsec:mechanisms}

\paragraph{Failure taxonomy.}
We label every graded run with one primary mode from the nine-mode
vocabulary of Appendix~\ref{app:taxonomy} and report the distribution by
tier in Table~\ref{tab:failure-modes}. Two modes are outcomes. A
reproduced run recovers the pinned number on the authors' released
artifact and data, and a run outside tolerance executes a faithful pipeline
and lands short of the bar. The other seven are process failures. The agent
reimplements the method or a component of it and checks no stage of its
version against a reference it did not produce, loses the decisive rounds to
build and dependency failures in the released stack, measures an artifact
other than the one the dataset row names, runs an experiment other than the
one the claim is about, echoes a shipped constant in place of the number it
measured, spends its rounds on reading and probing and never launches the
experiment, or runs a working pipeline on working artifacts that ends before
the graded command yields a value. Appendix~\ref{app:taxonomy} gives each
mode's evidence signature, with transcript excerpts for three of them. One run
the auditor graded reproduced carries the wrong-artifact label, because the
dissection found its passing number was measured on a released checkpoint
other than the one the row pins, so the Reproduced row holds 72 runs against
73 audited reproductions, with the difference in the Run column.
% the one run: muse-run-2505.18456 (audit 8; adlm-medium/large 262B/524B
% checkpoints against a pinned 110B row).

\begin{table}[t]
\centering
\small
\begin{tabular}{@{}lcccc@{}}
\toprule
Mode & Run & Retrain & Reimplement & All \\
\midrule
Reproduced & 30 & 29 & 13 & 72 \\
Ran, outside tolerance & 24 & 37 & 15 & 76 \\
Reimplemented but did not check the result & 14 & 11 & 38 & 63 \\
Build and dependency failures & 13 & 6 & 5 & 24 \\
Wrong artifact measured & 18 & 9 & 11 & 38 \\
Wrong experiment & 8 & 14 & 24 & 46 \\
Echoed a shipped number & 7 & 2 & 0 & 9 \\
Never launched an experiment & 1 & 2 & 10 & 13 \\
Failed before any number was produced & 14 & 8 & 9 & 31 \\
\midrule
Graded runs & 129 & 118 & 125 & 372 \\
\bottomrule
\end{tabular}
\caption{Primary failure mode by tier over the 372 graded runs, in the order of
Appendix~\ref{app:taxonomy}.}
\label{tab:failure-modes}
\end{table}
% numbers of record (2026-09-05): ~/sweeps/paper-table-2026-09-05/
% resolved_modes.json = index.json field "mode" with two relabel passes
% applied: 29 runs (25 "other" plus 4 label/verdict conflicts; evidence in
% result.json) and the 7 outside-tolerance runs with a disqualified verdict
% (4 moved; evidence in relabel_pass2.txt). Rows are the slugs reproduced-clean,
% near-miss-partial, reimplement-without-validating, environment-fights,
% artifact-provenance-mismatch, scope-substitution, stale-artifact-reliance,
% procrastination/wall-kill, killed-before-the-number.

\paragraph{Where the failures occur.}
Reimplemented but did not check the result is the most common process
failure, at 63 of 372 runs, and 38 of those fall at Reimplement. Together with
the wrong-experiment row it accounts for 62 of the 125 Reimplement-tier
runs, and the share is similar across agents, from 12 of 30 for Qwen3.6-27B to
18 of 33 for MiniMax-M2.7. Two modes sit almost entirely at one tier. Echoing
a shipped number needs released code and does not occur at Reimplement, and
never launching an experiment occurs ten times at Reimplement against once at
Run. Two modes concentrate in one agent each
(Table~\ref{tab:failure-modes-by-agent}). MiniMax-M2.7 holds 11 of the 24
build and dependency failures and Qwen3.6-27B holds 19 of the 31 runs that
failed before producing a number, while wrong-artifact measurements split
between MiniMax-M2.7 and Muse Spark 1.2 at 13 and 15 of 38. The build failures
depend on the host as well as the agent. Five of MiniMax-M2.7's eight Run-tier
cases trace to packages without aarch64 wheels on our GH200 nodes, and
DeepSeek-V4 met the same packaging gaps and split on how it adapted: on one
paper it rebuilt the package from source after the PyPI wheel failed at import
against the installed torch and, when the authors' scorer exceeded the
node's memory limit, rewrote it and checked the
rewrite against the original (Appendix~\ref{vig:dsv4-run-2503.18430}), and
on another it replaced the package by an unvalidated port that the auditor
disqualified.
% sources: resolved_modes.json for every count. aarch64 attribution: the five
% are 2504.20571, 2506.20671, 2505.24873, 2505.10978, 2505.14827 per
% notes/Analysis/2026-07-20 Easy Sweep 2652648 (MiniMax repair).md:95-103,145
% (pre-pinned-grade era; mode reasoning is transcript-based). The same note
% attributes 2503.18430 to a run_gpu allocation cap of ~3 minutes in our own
% harness at the time (8 killed mmcv builds), a harness confound to disclose in
% Limitations. DSv4 adaptation split: notes/Analysis/2026-08-11 Easy Sweep
% 2883229+2889575 (DSv4, pinned Sonnet).md:52 (2503.18430 valid chunked scorer;
% 2505.14827 unvalidated port, disqualified).

\paragraph{What the successful runs do.}
Most runs that recover the target result run the authors' own entrypoint. The
dissection records describe it for 58 of the 73, against 160 of the 299 runs
that fail. Where they patch released code they disclose the patch and show
that the graded computation is unchanged, and where the method has to be
rebuilt they check the rebuilt component against a quantity the paper reports
before spending compute on it. Reduced-scale smoke tests separate the two
groups only weakly, since the records mention them in 45 of the 73 successful runs
and 148 of the 299 failed ones. Appendix~\ref{app:annotated-runs} shows these
behaviors at the round level in ten annotated runs: three repair a defect in
the released code before the graded command runs, three check a host
substitution against the authors' own path, one reports a shortfall as
measured, and three put a different method or arm in the graded slot.
% sources: dissection records (analysis.* fields of the run records) read
% 2026-09-05: smoke test mentioned in 45/73 audited-
% reproduced and 148/299 failed records; reuse of released entrypoint in 58/73
% vs 160/299. Report lines: notes/Analysis/2026-08-03 Medium Sweep 2690187
% (MiniMax, pinned Sonnet).md:255-266; 2026-08-24 Hard Sweep muse_spark-
% 20260820T205201Z (Muse Spark 1.2, pinned Sonnet).md:198-209.

\subsection{Compute does not bind reproduction}
\label{subsec:compute}

\paragraph{Score falls with the paper's compute band.}
Reproduction scores fall as the paper's compute band grows, even though no
band comes close to exhausting the grant we assign it
(Table~\ref{tab:band-compute}). Mean score falls with band for each of the
four agents, and the 42 runs in the 96 H100-hour band spend 6.5\% of the
compute granted to them. The bands hold 59, 29, and 12 papers, and 99 of the
100 produced at least one run, so the 96-hour cells rest on 9 to 12 runs per
agent. The gradient tracks the scale of the experiment the paper demands.
Agents handle short evaluation-style experiments well, loading a released
checkpoint, running an inference pass, and recomputing a metric, but they rarely
carry a full training run through to the end. At Retrain, where the training
itself is the missing step, 7 of the 15 runs in the 96 H100-hour band spent
under 5\% of their grant, and 14 of the 15 did not reproduce. Seven of the 42
runs in the band ran a shrunken version of the paper's training and reported a
number from it, and six of the seven stated the shortfall.
% 96-band from index.json: 42 runs = 15 Retrain + 27 Reimplement (no Run-tier
% paper sits in the band); Retrain spent/budget < 0.05: 7/15, reproduced 1/15
% (muse-retrain-2508.21046); Reimplement < 0.05: 22/27. Pooled 29/42 is
% confounded with Reimplement having no code to launch, so it is not printed. Shrunken-training runs, read from the dissection records 2026-09-05:
% dsv4-retrain-2504.12463, dsv4-retrain-2505.02391, dsv4-retrain-2506.19839,
% qwen3-retrain-2504.12463, muse-retrain-2506.19839, muse-reimplement-
% 2506.02882, minimax-retrain-2504.12463 (the one that did not state the
% shortfall).

\begin{table}[ht]
\centering
\small
\begin{tabular}{@{}lccc@{\hspace{2.5em}}ccc@{}}
\toprule
& \multicolumn{3}{c@{\hspace{2.5em}}}{Mean score} & \multicolumn{3}{c}{Grant spent (\%)} \\
\cmidrule(l{1.2em}r{3em}){2-4} \cmidrule(lr){5-7}
Agent & 8 & 32 & 96 & 8 & 32 & 96 \\
\midrule
DeepSeek-V4 & 6.21 & 5.67 & 4.44 & 43.3 & 43.9 & 16.7 \\
Qwen3.6-27B & 4.18 & 3.79 & 2.22 & 31.8 & 20.5 & \phantom{0}2.6 \\
MiniMax-M2.7 & 3.62 & 2.50 & 1.92 & 27.3 & 11.9 & \phantom{0}2.3 \\
Muse Spark 1.2 & 5.91 & 4.55 & 3.08 & 39.5 & 32.6 & \phantom{0}6.1 \\
\midrule
All agents & 4.97 & 4.04 & 2.86 & 35.4 & 26.4 & \phantom{0}6.5 \\
\bottomrule
\end{tabular}
\caption{Mean reproduction score and share of the granted GPU-hours spent.}
\label{tab:band-compute}
\end{table}
% numbers of record (2026-09-03): pinned claude-sonnet-5 grades over the 12
% sweeps of record (the 9 in tools/anon_viewer/manifest.json plus muse_spark
% 20260814T135227Z / 20260816T133804Z / 20260820T205201Z), 372 runs. Band = the
% run's budget (agrees with eval_100 h100_band on every run); spent = sum of
% spent_h100 over sum of budget per cell; Papers = eval_100.jsonl h100_band
% counts. Per-agent n by band: DSv4 57/21/9, Qwen3.6 57/24/9, MiniMax 58/28/12,
% Muse Spark 56/29/12; 8-band 228 runs, 32-band 102, 96-band 42. Bands are
% named by their ceiling on the 0-8 / 8-32 / 32-96 ladder of Appendix E. One
% 96-band run reproduces (Muse Spark, Retrain, 2508.21046). Recompute:
% tools/anon_viewer/concordance.py band rows.

\section{Limitations}
\label{sec:limitations}

Discuss limitations honestly. This can include dataset limitations, compute constraints, assumptions, failure cases, or scope boundaries.

\section{Conclusion}
\label{sec:conclusion}

Summarize the paper and its implications.

\subsection*{Reproducibility statement}

\begin{itemize}
\item \textbf{Harness.} Appendix~\ref{app:harness} specifies the agent loop,
budget enforcement, the sandbox, and the prompts in full.
\item \textbf{Models.} Appendix~\ref{app:models} pins each evaluated model to a
served revision, or for the hosted model to an API identifier and an access
window, and states the serving configuration it ran under.
\item \textbf{Grading.} Appendix~\ref{app:rubric} gives the audit rubric,
frozen before any reported sweep was graded, and
Appendix~\ref{app:auditor-validation} bounds how much of a reported score is
grader noise.
\item \textbf{Dataset.} Appendix~\ref{app:dataset-schema} documents every field
and both splits.
\item \textbf{Artifacts.} Code, batch scripts, and the run bundles
are submitted as anonymized supplementary material, and
Appendix~\ref{app:running} gives the commands that serve a model, run a
paper, and grade a run. The runs behind the reported numbers can also be
read at \url{https://reclaim-traces.vercel.app}, an anonymized viewer that
shows each transcript verbatim with paths and identifiers redacted, together
with its audit score and its primary failure mode. The anonymized code
repository is linked from footnote~\ref{fn:artifacts} on the first page.
\end{itemize}

An agent rollout is sampled and interacts with a live machine, so a rerun does
not recover our transcripts turn for turn. The run bundles make the
grading stage reproducible from our artifacts where the rollout stage is not.

\subsection*{AI use statement}

Large language models are the object of study in this work. The agent models
under evaluation and the model that grades their reports are described in
Appendices~\ref{app:harness}--\ref{app:auditor-validation}, and their use is a
property of the benchmark rather than author assistance. Our own use of
generative AI tools was as follows.

\begin{itemize}
\item \textbf{Dataset.} We used them to extract and normalize the per-paper
metadata that forms the released dataset, including the artifact-availability
signals and the pinned match targets. Every extracted field was reviewed by an
author before the dataset was frozen.
\item \textbf{Implementation.} We used them to implement parts of the harness,
the serving stack, and the analysis scripts.
\item \textbf{Grading.} We used them to grade agent reports and to assign
failure modes. That is a methodological choice, and
Appendix~\ref{app:auditor-validation} exists because it needs validating
rather than asserting.
\item \textbf{Writing and figures.} We used them to draft and edit sections of
this paper, to produce the plotting code behind its figures, and to search and
summarize related literature. Every citation was checked against the cited
paper by an author.
\item \textbf{Not used.} We did not use generative AI tools to generate
synthetic datasets, to develop theoretical models, to formulate or prove
mathematical claims, or for translation. The hypotheses this paper tests, the
availability-tiered design of the benchmark, and the audit protocol are the
authors' own, and generative AI tools did not propose or refine them.
\end{itemize}

We have reviewed all AI-assisted work, and we take responsibility for the
final content of this paper, including text, claims, and artifacts produced
with the aid of generative AI.

\subsubsection*{Acknowledgments}  % template: unnumbered third-level heading, last item before the references

% src: funding acknowledgement, kept verbatim by author directive 2026-08-30; own section by directive 2026-09-05 (grant rules)
This research used the DeltaAI advanced computing and data resource, which is
supported by the National Science Foundation (award OAC 2320345) and the State
of Illinois. DeltaAI is a joint effort of the University of Illinois
Urbana-Champaign and its National Center for Supercomputing
Applications~\citep{bode2025deltaai}.

\raggedbottom
\bibliography{iclr2027_conference}
\bibliographystyle{iclr2027_conference}

\clearpage  % appendices start on a fresh page
\appendix
% The appendix pages carry breakable verbatim boxes that cannot always fill a
% page, and flushbottom then stretches the heading skips around them.

\input{appendix/a_harness}
\input{appendix/b_models}
\input{appendix/c_rubric}
\input{appendix/d_auditor_validation}
\input{appendix/e_construction}
\input{appendix/f_dataset}
\input{appendix/g_taxonomy}
\input{appendix/h_runs}
\input{appendix/i_running}
\input{appendix/j_prompts}

\end{document}
