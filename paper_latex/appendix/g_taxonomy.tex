\section{Failure-Mode Taxonomy}
\label{app:taxonomy}

\subsection{Labeling procedure}

We assign each run's primary failure mode in a transcript dissection that runs
after the audit and separately from it, reading the ordered event transcript,
the final report, and the audit verdict, rationale, and flags on the run row.
The modes are not mutually exclusive as mechanisms, so a run takes exactly one
primary label for the dominant one, and a dissection never edits scores,
verdicts, or flags, so a mode label never moves a grade. No dissection uses an
availability-wall label, since curation removed those papers from the frozen
split (Appendix~\ref{app:construction}), and where a report asserts one we
relabel the run from its own evidence. We add a new mode only on evidence from
at least two runs. The taxonomy has nine modes with fixed names, and before
pooling we remap retired names, underscore spellings, and sweep-local labels
onto them. Thirty-six runs labeled outside the vocabulary or against the
pinned verdict were re-read against the definitions of Table~\ref{tab:taxonomy}
and assigned by the agent's recorded behavior. The labels folded this way are
under-determined-target, scope-collapse,
unavailability-concluded-from-prose, context-or-round-exhaustion, fabrication
and fabrication-or-provenance-break, report-serialization-fault and
report-serialization-collapse, tool-call-format-collapse,
protocol-drift-direction-flip, eval-protocol-shopping, verified-non-attempt,
and success. Eleven of them had an in-vocabulary label disagreeing with the
pinned verdict, seven of those outside-tolerance on a disqualified run, and
four kept their label because the flag concerned only the run's self-report.
Figure~\ref{fig:failure-modes} and Table~\ref{tab:failure-modes-by-agent} use
the plain-language names beneath the slugs in Table~\ref{tab:taxonomy}, in the
same order.
% relabel passes 2026-09-05: ~/sweeps/paper-table-2026-09-05/result.json (pass
% 1, 29 runs) and relabel_pass2.txt (pass 2, 7 runs); resolved_modes.json holds
% the final per-run labels.

\subsection{The failure modes}

The band column of Table~\ref{tab:taxonomy} gives the rubric score where the
frozen rubric text states one, and the auditor, which grades from the run's
evidence and has no access to the mode label, does not apply it as a rule.

\begingroup
\footnotesize
\begin{longtable}{@{}>{\raggedright\arraybackslash}p{3.05cm}>{\raggedright\arraybackslash}p{3.85cm}>{\raggedright\arraybackslash}p{4.15cm}p{1.1cm}@{}}
\toprule
Mode & Definition & Evidence signature & Band \\
\midrule
\endfirsthead
\multicolumn{4}{@{}l}{\footnotesize\itshape Table~\ref{tab:taxonomy}, continued.} \\[3pt]
\toprule
Mode & Definition & Evidence signature & Band \\
\midrule
\endhead
\midrule
\endfoot
\bottomrule
\noalign{\vskip 6pt}
\caption{The nine failure modes, with one primary mode assigned per run. The
band column gives the rubric score of Appendix~\ref{app:rubric} where the
frozen rubric text states one and reads -- otherwise.}
\label{tab:taxonomy}\\
\endlastfoot
\texttt{reproduced-\allowbreak clean}\newline \emph{Reproduced} &
The agent runs the authors' released artifact on the authors' data and the
graded quantity falls inside the pinned bar. &
A value computed in the run and traced to a command the transcript contains,
with the released code, checkpoint, and data as inputs. &
8--10 \\
\addlinespace
\texttt{near-\allowbreak miss-\allowbreak partial}\newline \emph{Ran, outside tolerance} &
The agent executes a faithful pipeline and produces a measured number short of
the bar. &
A completed execution of the pinned quantity with sound provenance, and a
measured value on the wrong side of the bar. &
6--7 \\
\addlinespace
\texttt{reimplement-\allowbreak without-\allowbreak validating}\newline \emph{Reimplemented but did not check the result} &
The agent writes its own version of the method or a component of it and
anchors no stage of it to a reference it did not produce itself. &
A from-scratch component in the run's own code, alongside a reference row the
paper supplies, most often its own baseline in the same table as the
target, that the transcript never checks against. &
4 \\
\addlinespace
\texttt{environment-\allowbreak fights}\newline \emph{Build and dependency failures} &
The decisive rounds go to build and dependency failures in the released stack
on the host machine, and the claim's experiment never reaches a valid number. &
Repeated install, compile, and ABI failures against the authors' pins, with
the same package families recurring across runs on the same host. &
-- \\
\addlinespace
\texttt{artifact-\allowbreak provenance-\allowbreak mismatch}\newline \emph{Wrong artifact measured} &
The agent measures a checkpoint, dataset, split, or scale other than the one
the dataset row names. &
A retrieved artifact whose own model card, README, or shipped example
disagrees with the dataset row, and a measurement taken on it. The
\texttt{wrong\_\allowbreak split\_\allowbreak scale\_\allowbreak dataset} flag names the same break. &
-- \\
\addlinespace
\texttt{scope-\allowbreak substitution}\newline \emph{Wrong experiment} &
The experiment that ran differs from the experiment the pinned claim is
about. &
A round where the agent names the pinned arm and chooses a different one, and
a graded number produced from the substitute. &
0 or 2 \\
\addlinespace
\texttt{stale-\allowbreak artifact-\allowbreak reliance}\newline \emph{Echoed a shipped number} &
The agent runs the right thing, gets a number that misses, finds a shipped
constant in the repository that passes, and reports the constant. &
The run's own executed value and the reported value differ, and the reported
value traces to a file the repository shipped. The \texttt{stale\_artifact}
flag names the same break. &
4 or 0 \\
\addlinespace
\texttt{procrastination/\allowbreak wall-\allowbreak kill}\newline \emph{Never launched an experiment} &
The agent spends its rounds on reading, retrieval, and probing, the pinned
pipeline never runs, and the run stops or is cut off with most of the band
unspent. &
Rounds dominated by search and setup, an own estimate that the pinned run
fits the band, and an exit with the allocation largely unspent. &
-- \\
\addlinespace
\texttt{killed-\allowbreak before-\allowbreak the-\allowbreak number}\newline \emph{Failed before any number was produced} &
The run produces no measurement of the pinned quantity. The environment and
the artifacts work, and the run ends before the graded command yields a
value. &
A retrieved artifact set and a pipeline the transcript shows running, with no
value for the pinned metric anywhere in the run bundle. &
3 \\
\end{longtable}
\endgroup

\begin{table}[ht]
\centering
\fontsize{8}{9.6}\selectfont
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lccccc@{}}
\toprule
Mode & DeepSeek-V4-Flash & Qwen3.6-27B & MiniMax-M2.7 & Muse Spark 1.2 & All \\
\midrule
Reproduced & 27 & 13 & 10 & 22 & 72 \\
Ran, outside tolerance & 26 & 16 & 7 & 27 & 76 \\
Reimplemented but did not check the result & 13 & 16 & 27 & 7 & 63 \\
Build and dependency failures & 1 & 8 & 11 & 4 & 24 \\
Wrong artifact measured & 6 & 4 & 13 & 15 & 38 \\
Wrong experiment & 9 & 11 & 16 & 10 & 46 \\
Echoed a shipped number & 1 & 1 & 4 & 3 & 9 \\
Never launched an experiment & 2 & 2 & 4 & 5 & 13 \\
Failed before any number was produced & 2 & 19 & 6 & 4 & 31 \\
\midrule
Graded runs & 87 & 90 & 98 & 97 & 372 \\
\bottomrule
\end{tabular}
\caption{Primary failure mode by agent.}
\label{tab:failure-modes-by-agent}
\end{table}
% numbers of record (2026-09-05): ~/sweeps/paper-table-2026-09-05/resolved_modes.json.

\subsection{Failure taxonomy examples}

The signature of \texttt{reimplement-without-validating} is a reference the
paper supplies that the run never checks against. On arXiv paper 2510.08177
the agent wrote its own version of the paper's baseline, scored it below the
value the paper reports for that same baseline, and kept building on its own
version.

\begin{agentturn}{Round 56, agent.\ Paper 2510.08177, MiniMax-M2.7, sweep 2936132.}
Best All: 40.82\% (ep 20)\\
Target (paper ProCo+MORE): 65.9\%\\
Paper CE baseline: 61.4\%\\
Our CE-only baseline: 54.21\%\\
Gap vs target: -25.08\%
\end{agentturn}

The third and fourth lines are the paper's own baseline and the agent's
reimplementation of it, and the disagreement, visible in the agent's own
closing numbers, persisted to the end of the run.

The signature of \texttt{scope-substitution} is a round in which the agent
names the pinned arm and then chooses a different one. On arXiv paper
2505.10039 the agent made that choice at round 6 before writing any code,
giving implementation cost as the reason, never measured the pinned arm, and
produced the graded number from the arm it chose instead.

\begin{agentturn}{Round 6, agent.\ Paper 2505.10039, DeepSeek-V4-Flash-0731, sweep 2918306.}
The EAP method shows the largest gap and is the cleanest to implement (single forward/backward pass, no iterative search).
\end{agentturn}

Neither arm had released code, so either arm required a port, and the agent
chose between them on implementation effort.

The signature of \texttt{environment-fights} is a version incompatibility in
the released stack that persists long enough to corrupt the run's own data. On
arXiv paper 2511.20906 an incompatible pair of robosuite and mujoco versions
silently broke the released absolute-action dataset converter, and the agent
had already trained on the corrupted output by the time it discovered the
break.

\begin{agentturn}{Round 233, agent.\ Paper 2511.20906, DeepSeek-V4-Flash-0731, sweep 2918306.}
This is the smoking gun: **the saved abs dataset is broken** -{}- the abs actions don\textquotesingle t reproduce the expert trajectory even from the correct initial state.
\end{agentturn}

The defect is in the released code as it runs on this host, and the run
exhausted its budget on it.
